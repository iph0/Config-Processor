users:
  perlman:
    password: { var: passwords.system.perlman }
    home_dir: "/home/perlman"
    group:    "administrators"

  raptor:
    password: { var: passwords.system.raptor }
    home_dir: "/home/raptor"
    group:    "administrators"

  overlay: { include: users/* }
